# defines block experiment to run in the configured order

ExampleBlock1:
  module: st_dash_template.experiment.demo.example_block

ExampleBlock2:
  module: st_dash_template.experiment.demo.example_block

ExampleVisBlock:
  module: st_dash_template.experiment.demo.example_vis_block
